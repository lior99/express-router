version: '2'
services:
  web:
    container_name: docker-node-app
    build: .
    ports:
      - 8080:8080
    links:
      - mongo

  mongo:
    container_name: mongo
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - ./data:/data/db
